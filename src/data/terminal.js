export const terminalCode = [
    '<Button variant="primary" />',
    'const App = () => <Layout />;',
    'useState({ theme: "dark" })',
    '<ProjectCard data={projects[0]} />',
    'import { motion } from "framer-motion"',
    'tanstackQuery({ queryKey: ["skills"] })',
    'tailwind.config.js — content: ["./src/**/*.{jsx}"]',
];
